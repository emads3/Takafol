<%# <li class="col-md-4 mb-xlg isotope-item rooms">
  <h5 class="mt-md mb-none">Standard Room</h5>
  <div class="room-suite-info">
    <ul>
      <li><label>BEDS</label> <span>1 Double Bed</span></li>
      <li><label>OCCUPANCY</label> <span>2 Persons</span></li>
      <li><label>SIZE</label> <span>40 sqm.</span></li>
      <li><label>VIEW</label> <span>Porto Bay</span></li>
      <li><label>RATES FROM</label> <strong>USD 199</strong></li>
      <li>
        <a href="#" class="room-suite-info-detail" title="">
          View Details<i class="fa fa-long-arrow-right"></i>
        </a>
        <a href="#" class="room-suite-info-book" title="">Book Now</a>
      </li>
    </ul>
  </div>
</li> %>

<%
def cutter (x)
     i=0
    counter=x.split.count
    $a=[]
    while i < counter  do
        $y=x.split(' ')[i][0].capitalize
        i +=1
        $a.push($y)
     end
     $a.join(".")
end
%>

<div class="container">
	<div class=row>
		<% Case.all.each do |c| %>
		<% if  !c.charities.empty? && c.donors.empty? %>
        <% if c.charities_cases.last.state == "protected" && c.amount_needed >= c.amount_obtained%>
		<div class="col-md-4">
			<div class="featured-box featured-box-quaternary mt-sm" style="height: 377px;">
				<div class="box-content">
					<div class="row">
						<div class="col-md-12">
							<%# <i class="icon-featured fa fa-cogs m-none"></i> %>
							<%if !charity_signed_in?%>
							<h2 class="mb-lg"><%= cutter(c.name)%></h2>
							<% else %>
							<h2 class="mb-lg"><%= c.name%></h2>
							<% end %>
							<h6 class="mb-lg">Job: <%= c.job %></h6>
							<h6 class="mb-lg">Children: <%= c.children_num%></h6>
							<hr>
							<p><%= c.description %></p>
							<div class="divider divider-small divider-small-center">
								<hr>
							</div>
							<button class="btn btn-quaternary mr-xs mb-lg" type="button">Learn More</button>
						</div>
					</div>
				</div>
			</div>
		</div>
		<%end%>
		<%end%>
		<%end%>

	</div>
</div>



